# TalentGraph API

Express API for the TalentGraph application. Render deploys this service from the `server` root directory.

Deployment verification: GitHub Actions triggers Render through the service deploy hook after required checks pass.
